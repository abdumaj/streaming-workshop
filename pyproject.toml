[project]
name = "streaming-workshop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "abdumaj", email = "167322727+abdumaj@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "kafka-python>=3.0.11",
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
]

[project.scripts]
streaming-workshop = "streaming_workshop:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
